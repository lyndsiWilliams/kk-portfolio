// Package imports
import axios from 'axios';
// Styling
import { Button, Modal, Form, Input, Radio } from 'antd';

const BlogForm = () => {
  return (
    <div className="blog-form">
      This is the BlogForm.
    </div>
  );
};

export default BlogForm;
